# yarn-berry-attack

Yarn Berry (v2+) lockfile pinning `flat-cache@6.1.24` from the Aug 4, 2026 wave.
Berry uses `name@npm:version` descriptors and `version: x` instead of
`version "x"`; both dialects go through the same transform.
